Dockerfile = configuration file pur le docker
-- Comprend requirements, ffmpeg

Chercher les modifications de V lombart

Serveur utilisé : waitress
-- C'est lui qui lancer le wsgi

FLASK_ENV = deployement ou autre chose ? Important ?

Installer docker sur ma machine et lancer la commande de readme (changer le v20100 pour mon propre repo)

Si je vérifie en Python normal, vérifier mes variables d'environnement (fichier dockerfile)



Le gitconfig de V Lombart (attention branche par défaut c'est main, pas master)

Après avoir push, Docker construit l'image automatiquement
ENSUITE il faut tagger l'image : soit avec gittag et push le tag, soit dans Gitlab 'Reposo -> Tags", donner un n° (sans importance), puis create tag
puis dans CI/CD->pipeline, nouvelle ligne ajoutée : cliquer sur le bouton play (ou sur le "passed", puis "k8s deploy...".



gitconfig Lombart :
[core]
repositoryformatversion = 0
filemode = true
bare = false
logallrefupdates = true
ignorecase = true
precomposeunicode = true
[remote "github"]
url = https://github.com/GaelPouxMedard/Epimap
fetch = +refs/heads/*:refs/remotes/github/*
[branch "main"]
remote = origin
merge = refs/heads/main
[remote "origin"]
url = https://git.msh-lse.fr/epimap/epimap.git
fetch = +refs/heads/*:refs/remotes/origin/*